{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PlannerRequest",
  "description": "Request from a planner to the AWI runtime. Must use semantic action names and args, never CSS selectors.",
  "type": "object",
  "required": ["action", "args"],
  "properties": {
    "action": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*\\.[a-z][a-z0-9_]*$",
      "description": "Semantic action name (e.g. 'invoice.create'). Must be dot-separated identifier."
    },
    "args": {
      "type": "object",
      "description": "Arguments for the action, keyed by field name."
    },
    "confirmed": {
      "type": "boolean",
      "description": "Whether the user has explicitly confirmed this action."
    }
  },
  "additionalProperties": false
}
